#
# For a description of the syntax of this configuration file,
# see misc/tools/kconfig-language.txt.
#

if NETUTILS_MQTT

config EXAMPLES_MQTTCLIENT
	bool "MQTT Client Example"
	default n
	---help---
		Enable the MQTT client example

endif
