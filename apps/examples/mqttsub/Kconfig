#
# For a description of the syntax of this configuration file,
# see misc/tools/kconfig-language.txt.
#

if NETUTILS_MQTT

config EXAMPLES_MQTTSUB
	bool "MQTT Client Subscriber Example"
	default n
	---help---
		Enable the MQTT client subscriber example

endif
